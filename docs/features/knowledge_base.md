Knowledge bases are a way to store files and other stuff.

Ok what can I load on a knowledge base:
- a file
- a directory (but how)
- a zip
- google drive link to a folder or to a file (need authentication)
- a note that someone wrote

For sure before loading a bunch of stuff, it should check number of files and suff like that,
If google, i guess it should be kept in "sync". for now maybe every day it checks if there is something new. A google file is an external idea.
File are saved in batches.
The only important thing of a Knowledge base is that it stores file. for now let's make it super imple
